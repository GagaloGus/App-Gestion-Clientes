import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apptwin.alizzia',
  appName: 'Gestion Clientes Alizzia',
  webDir: 'dist/app-gestion-clientes/browser'
};

export default config;
